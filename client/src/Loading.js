import './Loading.css';

function Loading({props}) {

    // do stuff with props

    return (
        <div id="loadingContainer">
            <p className="bounceText">
                Loading...
            </p>
        </div>
    );
}
  
export default Loading;